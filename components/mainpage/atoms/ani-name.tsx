import React from 'react';
import styled from '@emotion/styled';

const AniName: React.FunctionComponent = () => {
  return <AniNameSpan>ONE PIECE</AniNameSpan>;
};

const AniNameSpan = styled.span`
  color: gray;
  font-size: 16px;
`;

export default AniName;
