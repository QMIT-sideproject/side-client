import styled from '@emotion/styled';

const RelationTitle = () => {
  return <StyledRelationTitle>Relations</StyledRelationTitle>;
};

const StyledRelationTitle = styled.h3`
  font-size: 18px;
  margin-bottom: 20px;
`;

export default RelationTitle;
